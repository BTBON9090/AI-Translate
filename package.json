{
  "name": "ai-translate",
  "version": "2.1.2",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:browser": "node tests/browser.cjs",
    "check": "node --check connection.js && node --check background.js && node --check content.js && node --check protocol.js && node --check providers.js && node --check popup/popup.js && node --check popup/i18n.js",
    "build": "python3 scripts/package.py",
    "test:connection": "node tests/connection-browser.cjs"
  },
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
