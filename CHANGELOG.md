# 2.1.2 — 2026-09-10

- 修复手动恢复原文后，被“始终翻译”延迟检查重新开启整页翻译的竞争：取消待执行检查，并使尚未完成的旧设置读取失效。当前 URL 保持暂停，进入其他页面或重新开启时恢复自动翻译。
- 隔离划词按钮与结果卡片的点击事件，过滤悬浮按钮、输入框和语言选择操作；悬浮按钮不响应网页脚本模拟点击、组合快捷键、键盘连发及取消的拖动。
- 网页面板使用明确的开启/关闭消息，重复消息不会反向切换；隐藏面板和重复点击不能触发整页翻译。网页队列只在网页翻译处于开启状态时消费任务。
- 输入翻译的“精细解释”按当前界面语言释义：中文界面使用中文，英文界面使用英文；普通翻译继续使用所选目标语言。
- 默认输入框缩小为 76px，保留手动拉高；Cmd / Ctrl + Enter 提交翻译，输入法确认和按键连发不提交。
- 保留现有 V3 段落缓存，不清空已有翻译。

验证：先复现旧版恢复原文后重新翻译 5 个段落的问题，再验证修复；17 项核心测试、浏览器交互回归和缺失后台接收端回归通过。交互测试覆盖按钮重叠、手动暂停、语言选择、两种快捷键、输入法、中英文解释语言与明确开启/关闭。使用本地模拟接口和测试 Key。

# 2.1.1 — 2026-09-10

- 修复模型检测对 service worker 消息接收端的依赖：检测从插件面板直接访问当前 API 地址，后台未就绪时不会将全部服务商误报为连接失败。
- 新增后台状态和版本握手；缺失接收端或版本不一致时显示独立恢复提示，可保存配置后重新加载扩展。
- 新增“阿里云百炼 · Token Plan 套餐”，预设与控制台一致的套餐 Base URL；将按量付费北京/新加坡选项标注清楚。
- 模型检测支持常见列表格式。已知官方接口不提供列表时，仅展示标明未验证的官方参考模型；认证、额度和网络错误仍明确报错。不会因为选中了某服务商就向其他域名发送 Key。
- 新增独立“测试此模型连接（少量用量）”：仅用户点击时生成一个上限 32 输出 token 的测试请求，验证选定模型和协议是否被接口接受。
- 检测期间更换地址、服务商、Key 或协议会取消旧请求，避免旧模型列表覆盖新配置。
- 保留 2.1.0 的 V3 段落缓存，此次更新不更改提示词版本或清空这些缓存。

验证：17 项核心测试、原有浏览器回归及独立缺失后台接收端的复现场景通过。使用模拟服务商接口和测试 Key；未使用真实用户 Key 验证套餐权限。

# 2.1.0 — 2026-09-09

- 输入翻译：默认打开文本面板，支持目标语言、单词多义释义、句子翻译、精细解释、停止、复制以及 Ctrl / Command + Enter。
- 缓存：每段独立持久化，跨页面、标签页和浏览器重启复用；重叠批次合并相同段落，仅请求未命中的内容。按服务地址、协议、模型、模式、语言、上下文和提示词版本隔离。
- 资源控制：全局最多 4 个模型请求，每页 2 个并发；后台页面暂停启动网页任务；扫描切片让出主线程，跳过插件 DOM 和纯样式变化；缓存最多 4,000 条、6 MiB、90 天。
- 正确性：批次使用 JSON 数组并校验数量；截断、空响应、协议错误和超时不缓存；扩展更新后不自动刷新用户网页。
- 悬浮按钮：左右贴边、Pointer Events 拖动、位置记忆、窗口边界修正、键盘操作和减少动效支持。
- 划词：原文预览、语境消歧、按需解释、结果复用、Escape 关闭、尺寸自适应和关闭时清理监听器。
- 模型连接：真实 URL + Key 检索模型，支持 Anthropic 分页、自定义模型 ID 和错误反馈；加入 OpenAI、Anthropic、百炼新加坡入口，明确工作空间地址。
- 中英文：补齐面板、提示、模型连接和交互状态文案；切换界面语言不修改翻译目标语言。

旧版按整批文本计算的缓存无法可靠拆回原文段落，升级后会清理 V2 缓存。首次访问可能重新翻译一次，此后使用新的逐段缓存。更换翻译设置或缓存过期会按对应配置重新生成。

验证使用本地模拟模型接口与独立 Chromium 扩展环境；真实服务商的密钥、余额及模型推理权限需使用用户的实际账号检测。
